# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -Dorg.gradle.process.env.PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# ==============================================================================
# Maven Central Publishing Configuration (Sonatype Central Portal API)
# ==============================================================================
# 参考文档: https://central.sonatype.org/publish/publish-portal-api/

# Sonatype Central Portal API 端点
# API基础URL
mavenCentral.apiBaseUrl=https://central.sonatype.com/api/v1/publisher

# 用户令牌配置
# 方式1: 直接使用base64编码的token (推荐)
# 在Central Portal的Account页面生成User Token后，格式为: username:password
# 使用命令生成: printf "username:password" | base64
#mavenCentral.userToken=WDN0ekZPOkdKT3hwZ3lUejJDQ3E4VDFFb3NGV1p5VERrYWd2NDBUbw==

# 方式2: 分别配置用户名和密码 (可选，如果使用方式1则不需要)
mavenCentral.username=HUsZci
mavenCentral.password=i5fMPf6Ei6eh2Ctmky1RhMljNYLE4endd

# 发布类型配置
# AUTOMATIC: 部署通过验证后自动发布到Maven Central
# USER_MANAGED: 部署通过验证后需要手动在Portal UI中发布 (默认)
mavenCentral.publishingType=AUTOMATIC

# 部署名称 (可选，用于标识部署)
# 如果不设置，将使用bundle文件名
 mavenCentral.deploymentName=joytalk-plug

# 项目坐标信息 (用于POM文件生成)
# Group ID (命名空间，通常与域名反向对应)
# 已验证的命名空间: io.github.jt-plug
mavenCentral.groupId=io.github.jt-plug

# Artifact ID (库名称)
mavenCentral.artifactId=joytalk

# 版本号
mavenCentral.version=1.0.0-beta07

# 项目信息 (用于POM文件)
mavenCentral.name=JoyTalk Library
mavenCentral.description=A library for the JoyTalk project
mavenCentral.url=https://github.com/jt-plug/JoyTalk
mavenCentral.license.name=Apache-2.0
mavenCentral.license.url=https://www.apache.org/licenses/LICENSE-2.0.txt
mavenCentral.developer.id=jt-plug
mavenCentral.developer.name=jt-plug
mavenCentral.developer.email=higiao12@outlook.com

# SCM信息
mavenCentral.scm.url=https://github.com/jt-plug/JoyTalk.git
mavenCentral.scm.connection=scm:git:git://github.com/jt-plug/JoyTalk.git
mavenCentral.scm.developerConnection=scm:git:ssh://git@github.com/jt-plug/JoyTalk.git

# GPG签名配置
# 是否启用签名 (true/false，发布到Maven Central需要签名)
mavenCentral.signing.enabled=true
# 密钥ID (使用短格式，后8位)
mavenCentral.signing.keyId=F60455A7